defaults:
  - pg_base_baseline

name: pg_production_twoplayer_id_cot
scenario: pg_production

env:
  name: pg_production_twoplayer_id_cot

agent:
  cot_prompt: think_step_by_step
  reveal_identity: true